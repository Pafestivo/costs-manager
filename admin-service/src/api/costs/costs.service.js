/**
 * Costs service (STUB)
 * temporary implementation until costs process is completed
 */

/**
 * get total costs for a specific user
 * @param {number} userId
 * @returns {number}
 */
export async function getTotalCostByUserId(userId) {
    // TODO: replace with real implementation once costs service is ready
    return 0;
}
